<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FutsalHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/bgblack</item>
        <item name="colorPrimary">@color/green</item>
        <item name="colorControlActivated">@color/light_white</item>
        <item name="android:editTextColor">@color/white</item>
        <item name="toggleCheckedStateOnClick">@color/green</item>
    </style>

    <style name="Theme.FutsalHub" parent="Base.Theme.FutsalHub" />

</resources>
